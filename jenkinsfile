
 

node {
   
   stage('SCM Checkout'){
	git 'https://github.com/ami123k/user'
   
   }
     
stage('compile-Package'){
def mvnHome = tool name: '', type: 'maven'
	   
	   sh "${mvnHome}/bin/mvn package"
   }
   
}
